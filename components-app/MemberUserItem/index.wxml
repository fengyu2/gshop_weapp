<block>
    <view class="pb-member-user-item">
        <view class="time">{{item.created_at}}</view>
        <image class="avatar" src="{{item.avatar}}"></image>
        <view class="username">{{item.username}}</view>
    </view>
</block>